name: Release JetBrains Plugin

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: "Build plugin zip and skip publishing, tagging, and GitHub Release"
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  release:
    name: Build and publish
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('apps/jetbrains/**/*.gradle.kts', 'apps/jetbrains/gradle.properties', 'apps/jetbrains/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Compute version
        id: version
        run: |
          MAJOR_MINOR=$(grep '^pluginVersion=' apps/jetbrains/gradle.properties | cut -d= -f2 | cut -d. -f1-2)
          VERSION="${MAJOR_MINOR}.${{ github.run_number }}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "tag=jetbrains-v${VERSION}" >> "$GITHUB_OUTPUT"
          echo "Computed version: ${VERSION}"

      - name: Build plugin
        working-directory: apps/jetbrains
        run: ./gradlew -PpluginVersion=${{ steps.version.outputs.version }} buildPlugin --no-daemon

      - name: Verify plugin zip exists
        run: |
          VSIX="apps/jetbrains/build/distributions/codesetu-jetbrains-${{ steps.version.outputs.version }}.zip"
          if [ ! -f "$VSIX" ]; then
            echo "::error::Expected plugin zip not found at $VSIX"
            ls apps/jetbrains/build/distributions/ || true
            exit 1
          fi
          echo "Built: $VSIX"

      - name: Publish to JetBrains Marketplace
        if: ${{ !inputs.dry-run }}
        working-directory: apps/jetbrains
        env:
          JETBRAINS_MARKETPLACE_TOKEN: ${{ secrets.JETBRAINS_MARKETPLACE_TOKEN }}
        run: ./gradlew -PpluginVersion=${{ steps.version.outputs.version }} publishPlugin --no-daemon

      - name: Configure git for tagging
        if: ${{ !inputs.dry-run }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Tag the release commit
        if: ${{ !inputs.dry-run }}
        run: |
          git tag "${{ steps.version.outputs.tag }}"
          git push origin "${{ steps.version.outputs.tag }}"

      - name: Create GitHub Release
        if: ${{ !inputs.dry-run }}
        uses: softprops/action-gh-release@v2
        with:
          name: JetBrains Plugin ${{ steps.version.outputs.tag }}
          tag_name: ${{ steps.version.outputs.tag }}
          files: apps/jetbrains/build/distributions/codesetu-jetbrains-*.zip
          generate_release_notes: true
          fail_on_unmatched_files: true

      - name: Job summary
        if: always()
        run: |
          {
            echo "## Release summary"
            echo ""
            echo "- Version: \`${{ steps.version.outputs.version }}\`"
            echo "- Tag: \`${{ steps.version.outputs.tag }}\`"
            echo "- Run number: \`${{ github.run_number }}\`"
            if [ "${{ inputs.dry-run }}" = "true" ]; then
              echo "- Mode: **dry-run** (no publish, no tag pushed, no GitHub Release)"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
