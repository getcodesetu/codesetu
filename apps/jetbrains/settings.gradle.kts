plugins {
  // Auto-downloads required JDK versions so developers don't need to pre-install them.
  id("org.gradle.toolchains.foojay-resolver-convention") version "0.8.0"
}

rootProject.name = "codesetu-jetbrains"
