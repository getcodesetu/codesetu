<idea-plugin>
  <id>ai.codesetu.codesetu</id>
  <name>CodeSetu</name>
  <vendor url="https://github.com/getcodesetu/codesetu">CodeSetu Maintainers</vendor>

  <description><![CDATA[
    <h2>CodeSetu — Open-source AI coding assistant for JetBrains IDEs</h2>

    <p>
      A privacy-aware, multi-provider Copilot / Cursor alternative for Indian developers,
      enterprises, public-sector teams, and air-gapped deployments.
      <b>Bring your own model.</b>
    </p>

    <h3>What you get</h3>
    <ul>
      <li><b>Chat tool window</b> — streamed, markdown-rendered replies with rendered code blocks, in a polished CodeSetu webview.</li>
      <li><b>Selected-code actions</b> — Explain, Refactor, Write Tests, Fix Bug, Add Docs, right from the editor context menu.</li>
      <li><b>Repo-aware context</b> — active file, selection, and cursor neighborhood are attached automatically (toggleable per message).</li>
      <li><b>In-chat provider &amp; model switcher</b> — change providers, base URLs, and models without leaving the chat.</li>
      <li><b>Workspace skills &amp; checks</b> — drop <code>.codesetu/skills/*.md</code> and <code>.codesetu/checks/*.md</code> files; they are folded into the system prompt.</li>
      <li><b>Multi-turn conversation history</b> — kept per session.</li>
    </ul>

    <h3>Works with</h3>
    <ul>
      <li><b>Sarvam</b> — Indic-aware hosted models.</li>
      <li><b>Hugging Face</b> — the hosted router (<code>router.huggingface.co/v1</code>), your dedicated Inference Endpoints, or self-hosted TGI. Any served chat model (Llama, Qwen, DeepSeek, Mistral, Gemma…) with your <code>hf_…</code> token.</li>
      <li><b>OpenAI-compatible</b> — Ollama, vLLM, SGLang, OpenRouter, LM Studio, any compatible endpoint.</li>
      <li><b>Self-hosted &amp; air-gapped</b> — bring your own URL. No outbound calls beyond the provider you configure.</li>
    </ul>

    <h3>Privacy &amp; security</h3>
    <ul>
      <li>API keys live in the OS <b>PasswordSafe</b> (keychain) — never in plaintext settings XML.</li>
      <li>Common secret files (<code>.env*</code>, <code>*.pem</code>, <code>*.key</code>, <code>secrets/</code>, …) are excluded from auto-collected workspace context.</li>
      <li>No CodeSetu telemetry. Your prompts go only to the provider you configured.</li>
      <li>100% open-source under Apache 2.0.</li>
    </ul>

    <h3>Quick start</h3>
    <ol>
      <li>Install the plugin and restart the IDE.</li>
      <li>Open the <b>CodeSetu</b> tool window (right dock).</li>
      <li>Click the <b>provider · model</b> chip in the composer → <b>Configure provider / endpoint…</b> → pick Sarvam, Hugging Face, or OpenAI-compatible, paste your key, and start chatting.</li>
    </ol>

    <p><b>Selected-code actions:</b> right-click any selection → <i>Explain / Refactor / Write Tests / Fix Bug / Add Docs with CodeSetu</i>.</p>

    <h3>Links</h3>
    <ul>
      <li><a href="https://github.com/getcodesetu/codesetu">Source on GitHub</a></li>
      <li><a href="https://github.com/getcodesetu/codesetu/blob/main/INSTALL.md">Install &amp; setup guide</a></li>
      <li><a href="https://discord.gg/sjVKU8cpC6">Community Discord</a></li>
      <li><a href="https://github.com/getcodesetu/codesetu/issues">Report an issue</a></li>
    </ul>
  ]]></description>

  <change-notes><![CDATA[
    <h3>0.4.0</h3>
    <ul>
      <li><b>Agent Mode</b> — a composer toggle that turns chat into an agent: it reads, edits, and runs commands to complete a task, then you review the result. Tools include read/write/edit files, run shell commands, search (grep / glob / list), task tracking, and IDE diagnostics.</li>
      <li><b>Inline approvals</b> — file edits and shell commands are approved right in the chat, with a diff preview for edits; read-only tools run automatically. Approve, Approve for session, or Deny.</li>
      <li><b>Project policy</b> — drop a committable <code>.codesetu/agent.json</code> to set <code>autoApproveCommands</code>, <code>denyCommands</code> (regex; deny wins), and <code>maxIterations</code>.</li>
      <li>Works with local / self-hosted models that emit tool calls as text (Ollama, llama.cpp). Agent Mode is off by default and needs a tool-calling-capable model.</li>
    </ul>
    <h3>0.3.1</h3>
    <ul>
      <li><b>"Context sent to AI" preview</b> — a collapsible panel under each turn showing the routed skill, your selected code, related snippets, and the full assembled payload, so you can see exactly what the model receives.</li>
      <li><b>"Thought for Ns" thinking panel</b> — when a model streams its reasoning, it's shown in a collapsible panel above the answer that opens while it thinks and collapses once the reply starts.</li>
      <li><b>Skills load from bundled <code>SKILL.md</code> files</b> — built-in skills now have a single source of truth instead of hard-coded copies.</li>
      <li><b>Composer UX</b> — Enter sends and Shift+Enter inserts a newline; the slash palette closes once you start typing your message.</li>
    </ul>
    <h3>0.3.0</h3>
    <ul>
      <li><b>Plan Mode</b> — composer toggle that asks the assistant for a numbered plan instead of code; "Approve &amp; Run" sends the canonical approval phrase and exits the mode. Toggle state persists across IDE restarts.</li>
      <li><b>AI Skills with slash palette</b> — type <code>/</code> in the composer to invoke <code>/plan</code>, <code>/explain</code>, <code>/refactor</code>, <code>/test</code>, <code>/indic</code>. Keyword auto-routing picks a skill from natural-language prompts (toggle in Settings → CodeSetu).</li>
    </ul>
    <h3>0.2.0</h3>
    <ul>
      <li>Chat tool window rendered with the CodeSetu webview design (markdown replies, model chip)</li>
      <li>Hugging Face provider support, plus in-chat provider/model switching (Sarvam, OpenAI-compatible, Hugging Face)</li>
      <li>API key stored securely in PasswordSafe; multi-turn chat history</li>
    </ul>
    <h3>0.1.0</h3>
    <ul>
      <li>Initial JetBrains plugin scaffold</li>
      <li>Adds CodeSetu menu group with a placeholder "Open Chat" action</li>
    </ul>
  ]]></change-notes>

  <depends>com.intellij.modules.platform</depends>

  <actions>
    <group id="ai.codesetu.MainMenu" text="CodeSetu" description="CodeSetu actions" popup="true">
      <add-to-group group-id="ToolsMenu" anchor="last"/>
      <action
        id="ai.codesetu.OpenChatAction"
        class="ai.codesetu.actions.OpenChatAction"
        text="Open Chat"
        description="Open the CodeSetu chat panel"/>
      <action
        id="ai.codesetu.DiagnoseProviderAction"
        class="ai.codesetu.actions.DiagnoseProviderAction"
        text="Diagnose Provider"
        description="Test the configured CodeSetu provider"/>
      <action
        id="ai.codesetu.SetupSpeechProviderAction"
        class="ai.codesetu.actions.SetupSpeechProviderAction"
        text="Setup Speech Provider"
        description="Configure the mic speech-to-text backend (Sarvam, OpenAI-compatible, HuggingFace)"/>
      <action
        id="ai.codesetu.ExplainSelectionAction"
        class="ai.codesetu.actions.ExplainSelectionAction"
        text="Explain Selection"
        description="Explain the selected code"/>
      <action
        id="ai.codesetu.RefactorSelectionAction"
        class="ai.codesetu.actions.RefactorSelectionAction"
        text="Refactor Selection"
        description="Suggest a focused refactor"/>
      <action
        id="ai.codesetu.WriteTestsForSelectionAction"
        class="ai.codesetu.actions.WriteTestsForSelectionAction"
        text="Write Tests for Selection"
        description="Write focused tests for the selected code"/>
      <action
        id="ai.codesetu.FixBugInSelectionAction"
        class="ai.codesetu.actions.FixBugInSelectionAction"
        text="Fix Bug in Selection"
        description="Find and fix a bug in the selected code"/>
      <action
        id="ai.codesetu.AddDocsToSelectionAction"
        class="ai.codesetu.actions.AddDocsToSelectionAction"
        text="Add Docs to Selection"
        description="Add useful documentation for the selected code"/>
      <action
        id="ai.codesetu.RevertLastAgentEditsAction"
        class="ai.codesetu.actions.RevertLastAgentEditsAction"
        text="Revert Last Agent Edits"
        description="Restore the files changed by the most recent CodeSetu agent turn"/>
      <action
        id="ai.codesetu.EditWithCodeSetuAction"
        class="ai.codesetu.edit.EditWithCodeSetuAction"
        text="Edit with CodeSetu"
        description="Rewrite the selection (or whole file) from an instruction, with a diff preview">
        <add-to-group group-id="EditorPopupMenu" anchor="last"/>
      </action>
    </group>
  </actions>

  <extensions defaultExtensionNs="com.intellij">
    <toolWindow
      id="CodeSetu"
      anchor="right"
      factoryClass="ai.codesetu.toolwindow.CodeSetuToolWindowFactory"/>
    <applicationConfigurable
      instance="ai.codesetu.settings.CodeSetuSettingsConfigurable"
      displayName="CodeSetu"/>
    <!--
      Adds the CEF flags required for the chat webview's mic button. Without
      this, getUserMedia throws NotSupportedError in the JCEF browser. See
      CodeSetuJBCefArgsProvider for the per-flag rationale and the README's
      "Voice in JetBrains" section for the user-facing trade-off.
    -->
    <jbCefAppRequiredArgumentsProvider
      implementation="ai.codesetu.toolwindow.CodeSetuJBCefArgsProvider"/>
    <!-- Ghost-text fill-in-the-middle completions as you type. -->
    <inline.completion.provider
      implementation="ai.codesetu.completion.CodeSetuInlineCompletionProvider"/>
  </extensions>
</idea-plugin>
